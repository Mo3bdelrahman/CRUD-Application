@model List<Department>
@{
    ViewData["Title"] = "Index";
}

<h1>Departments Index</h1>

<div>
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Operations</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Count > 0)
            {
                foreach (Department dept in Model)
                {
                    <tr>
                        <td>@dept.Id</td>
                        <td>@dept.Name</td>
                        <th>
                            <a action="Details" asp-controller="Department" asp-route-Id="@dept.Id" class="btn btn-primary">Details</a>
                            <a action="Update" asp-controller="Department" asp-route-Id="@dept.Id" class="btn btn-success"> Update</a>
                            <a action="Delete" asp-controller="Department" asp-route-Id="@dept.Id" class="btn btn-danger">Delete</a>
                        </th>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="3">No Departments yet</td>
                </tr>

            }
        </tbody>
    </table>
</div>
<div>
    <a asp-action="Create" asp-controller="Department" class="btn btn-outline-primary ">Create</a>
</div>